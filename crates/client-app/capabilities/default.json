{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main window: enables the frontend to use the Tauri event bus (core:event — listen/emit) plus the other default core plugin commands. Without this, listen() is denied by the v2 ACL and no backend events (transcode progress, player phases/frames, connection state, upload/fetch state) ever reach the webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
